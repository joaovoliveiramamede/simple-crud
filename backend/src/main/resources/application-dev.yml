server:
    port: 8081
spring:
    config: 
        activate:
            on-profile: dev
    application:
        name: backend
    datasource:
        url: jdbc:mysql://localhost:3307/simplecrud
        username: simplecruduser
        password: simplecrudpass
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQLDialect
